# 迁移到服务器 — 部署方案与可行性

> **状态**：方案已定稿，可按 §12 分阶段实施  
> **目标**：在一台服务器上通过 Docker Compose 全栈运行 8D KG 平台，复现本地「上传 → Pipeline → Codex 抽取 → 入图 → 查询」完整流程。  
> **与本地开发的区别**：本地是「本机进程 + SSH 隧道连远端 DB」；服务器是「全部容器化 + 内网服务名直连」，不再需要隧道。

---

## 1. 已拍板的架构选择

| 决策项 | 选择 | 说明 |
|--------|------|------|
| **部署拓扑** | **混合** | `app` 容器：FastAPI + Celery worker；`codex` 容器：独立 `codex-service` |
| **Codex 运行方式** | **容器内安装 Codex CLI** | `codex-service` 内执行 `codex exec`，与本地 `make codex-service` 一致 |
| **基础设施** | **全新 Compose 全栈** | 单机上一套 compose：PG / Neo4j / Redis / MinIO + 应用 + Codex |

> **说明**：`CLAUDE.md` / `AGENTS.md` 中「禁止 docker-compose」针对**本地开发**（避免在本机再起一套 DB）。服务器全量部署应在独立目录（建议 `deploy/`）维护 compose，与本地隧道流程分开。

---

## 2. 目标拓扑

```
                    ┌─────────────────────────────────────────┐
                    │           Single Server (Linux)         │
                    │  ┌───────────────────────────────────┐  │
                    │  │     docker-compose (内网 bridge)   │  │
                    │  │                                   │  │
  用户 ──HTTPS──▶    │  │  frontend (nginx) ──▶ app         │  │
                    │  │       │                │          │  │
                    │  │       │            ┌───┴───┐      │  │
                    │  │       │            │       │      │  │
                    │  │       │         postgres neo4j    │  │
                    │  │       │            redis  minio   │  │
                    │  │       │              │            │  │
                    │  │       │        CODEX_BASE_URL     │  │
                    │  │       └──────────────┼───────────▶│  │
                    │  │                      │     codex  │  │
                    │  │                      │   (CLI+    │  │
                    │  │                      │  :8787)    │  │
                    │  └──────────────────────┼────────────┘  │
                    │                         │               │
                    │                         ▼               │
                    │             External LLM Gateway        │
                    └─────────────────────────────────────────┘
```

### 2.1 服务清单（约 7～8 个容器）

| 服务名 | 职责 | 端口（示例） |
|--------|------|----------------|
| `postgres` | 元数据、chunks、审计、pgvector | 5432（仅内网） |
| `neo4j` | 知识图谱 | 7474 / 7687 |
| `redis` | Celery broker + 缓存 | 6379 |
| `minio` | 文档对象存储 | 9000 / 9001 |
| `app` | `uvicorn`（FastAPI）+ `celery worker` | 8000 |
| `codex` | `uvicorn app.codex_service` + **Codex CLI** | 8787 |
| `frontend` | 静态资源 + 反代 API（nginx） | 80 / 443 |
| `migrate`（可选） | `alembic upgrade head` 一次性 Job | — |

### 2.2 与本地 Makefile 的对应关系

| 本地命令 | 服务器容器 |
|----------|------------|
| `make tunnel-up` | **不需要**（DB 在同 compose 内） |
| `make backend` | `app` 容器内 uvicorn |
| `make worker` | `app` 容器内 celery worker |
| `make codex-service` | `codex` 容器 |
| `make frontend` | `frontend` 容器 |

---

## 3. 与现有代码的契合度

当前 Pipeline 已按「HTTP 解耦 Codex」设计，**混合拓扑几乎不需要改业务逻辑**。

### 3.1 调用链

```
上传文档
  → Celery Pipeline (s1…s6)
    → s4_extract
      → CodexClient (HTTP POST)
        → codex-service :8787 /extract
          → CodexExecRunner
            → `codex exec` + JSON Schema
              → 返回 ExtractionResult
    → s6_write → PostgreSQL + Neo4j
```

关键代码位置：

- `backend/app/pipeline/s4_extract.py` — 单次 LLM 抽取入口
- `backend/app/llm/codex_client.py` — 调用 `CODEX_BASE_URL`
- `backend/app/codex_service.py` — 本地 Codex HTTP 适配层
- `backend/app/services/codex_exec_runner.py` — 执行 `codex exec`
- `backend/app/pipeline/codex_skill_router.py` — skill 路由与模块选择
- `backend/app/llm/prompts.py` + `backend/app/llm/skill_templates/*.md` — skill 内容

### 3.2 容器内只需改配置（示意）

| 变量 | 本地典型值 | 服务器典型值 |
|------|------------|--------------|
| `DATABASE_URL` | `@localhost:5432` | `@postgres:5432` |
| `NEO4J_URI` | `bolt://localhost:7687` | `bolt://neo4j:7687` |
| `REDIS_URL` / `CELERY_*` | `@localhost:6379` | `@redis:6379` |
| `MINIO_ENDPOINT` | `localhost:9000` | `minio:9000` |
| `CODEX_BASE_URL` | `http://127.0.0.1:8787` | `http://codex:8787` |
| `CODEX_EXEC_WORKDIR` | 项目根目录 | 挂载进 codex 容器的代码路径 |

---

## 4. Skills 路径：可行性与边界

### 4.1 结论

| 做法 | 是否可行 |
|------|----------|
| 使用仓库内 `skill_templates/*.md` + `codex_skill_router` | **是，且已是现状** |
| 把 Cursor / IDE 的 `.codex/skills` 目录挂进容器当运行时 | **否，不建议** |
| 混合拓扑 + 容器内 CLI + 现有 prompt 模块 | **推荐** |

### 4.2 Skills 在生产里的实际形态

本项目的「skill」**不是** Cursor Agent Skills 运行时，而是：

1. `codex_skill_router.py` 根据章节 / chunk 信号选择 `prompt_modules`
2. `prompts.build_codex_system_prompt()` 把 `skill_templates/*.md` 拼进 system prompt
3. `codex exec --output-schema` 强制输出 `ExtractionResult` JSON

当前模块示例：

- `8d-report-extraction-core`（母 skill）
- `8d-actor-entity-typing`
- `8d-temporal-normalization`
- `8d-relationship-normalization`
- `8d-d2-event-extraction` / `8d-d4-root-cause-extraction` / `8d-d5-action-extraction`（按信号追加）

版本通过 `CODEX_SKILL_NAME` / `CODEX_SKILL_VERSION` 与 git 中的 md 文件管理即可。

### 4.3 Codex CLI 调用参数（已实现）

`codex_exec_runner.py` 使用非交互模式，适合容器：

- `--skip-git-repo-check`
- `--ephemeral`
- `-s read-only`
- `-C <workdir>`（项目根，含 lexicon / skill_templates）
- `--output-schema` + `-o` 输出文件

---

## 5. 逐项可行性评估

### 5.1 混合拓扑（app + codex 分离）— 可行

**优点**

- Codex 调用慢（默认超时 120s），与 API / worker 隔离，避免拖死 web 进程
- 可单独给 `codex` 容器加 CPU / 内存或水平扩展
- 与本地 `make backend` / `worker` / `codex-service` 三分法一致

**注意**

- `app` 容器需同时跑 **uvicorn + celery**（supervisor / s6 / 启动脚本），或在 compose 里用**同一镜像、两个 service**（`app-api` + `app-worker`）——逻辑上仍是「应用层混合、Codex 分离」。

### 5.2 容器内 Codex CLI — 可行，风险最高

实施前需确认：

| 项 | 说明 |
|----|------|
| CLI 安装 | npm 全局 / 官方安装脚本 / 私有基础镜像；**必须固定版本** |
| 鉴权 | API Key 或 `~/.codex` 配置挂载；容器内**不能**依赖交互式登录 |
| 模型路由 | `CODEX_EXEC_MODEL` 或 CLI 默认模型能否访问你的 LLM 网关 |
| 网络 | 容器能访问外网或内网网关（如现有 LiteLLM `117.62.232.51:14000`） |
| 资源 | 建议 `codex` 单独 limit（如 2 CPU / 4GB+） |

**建议保留兜底**：`LLM_FALLBACK_PROVIDER=litellm`，Codex 超时或不可用时 Pipeline 仍可完成（与 `.env.example` 一致）。

### 5.3 全新 Compose 全栈 — 可行

| 本地（现在） | 服务器（目标） |
|--------------|----------------|
| `localhost` + SSH 隧道端口 | Docker 服务名 + 标准端口 |
| 四个终端手动启动 | `docker compose up -d` |
| 远端 `/opt/8d-kg-infra/` 四容器 | 本 compose 内自建四库 |

**资源建议**（参考 `docs/ARCHITECTURE.md`）：

- 约 **7 vCPU / 12GB** 可跑通 MVP
- 若 Codex + Neo4j 同时高负载，建议 **16GB** 更稳
- 磁盘：PG ~50GB、Neo4j ~30GB、MinIO ~100GB（按数据量调整）

**持久化**：必须为 `postgres` / `neo4j` / `minio` / `redis` 配置 named volumes，避免 `docker compose down -v` 误删数据。

---

## 6. 环境变量对照（本地 → 服务器）

以下为方案级对照，实施时写入 `deploy/.env` 或 `deploy/.env.example`。

```bash
# === 数据库（服务名替代 localhost）===
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://kguser:***@postgres:5432/kg
ALEMBIC_DATABASE_URL=postgresql+psycopg://kguser:***@postgres:5432/kg

NEO4J_URI=bolt://neo4j:7687

REDIS_URL=redis://:***@redis:6379/0
CELERY_BROKER_URL=redis://:***@redis:6379/1
CELERY_RESULT_BACKEND=redis://:***@redis:6379/2

MINIO_ENDPOINT=minio:9000
MINIO_SECURE=false

# === LLM ===
LLM_PROVIDER=codex
LLM_FALLBACK_PROVIDER=litellm
LLM_PRIMARY_SOFT_TIMEOUT_SECONDS=30

CODEX_BASE_URL=http://codex:8787
CODEX_CLI_PATH=codex
CODEX_EXEC_WORKDIR=/app          # 挂载仓库根或 backend 父目录
CODEX_TIMEOUT_SECONDS=120

# LiteLLM 兜底（按实际网关地址填写）
LITELLM_BASE_URL=http://<gateway-host>:14000/v1
LITELLM_API_KEY=***

# === 应用 ===
ENV=production
LOG_LEVEL=INFO
CORS_ORIGINS_RAW=https://<your-domain>   # 或 http://服务器IP
SECRET_KEY=<openssl rand -hex 32>
```

---

## 7. 启动与验收顺序

1. 启动基础设施：`postgres` → `neo4j` → `redis` → `minio`（healthcheck 通过）
2. 执行迁移：`alembic upgrade head`（一次性 job 或 app 启动前）
3. 初始化 MinIO bucket（如 `kg-documents`）
4. 启动 `codex`，验证：`curl http://codex:8787/health` → `{"status":"ok"}`
5. 启动 `app`（API + worker），验证：`GET /api/v1/health` → postgres / neo4j / redis / minio 均为 ok
6. 启动 `frontend`
7. **端到端**：上传 EP2002 样例 docx → Pipeline succeeded → Neo4j 可查实体（参考 `backend/tests/test_e2e_live_pipeline.py`）

---

## 8. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| Codex CLI 在容器内无法非交互鉴权 | s4 全失败 | 先做 **Codex 镜像 POC**；保留 LiteLLM fallback |
| app 容器双进程管理不当 | worker 或 API 偶发挂掉 | 用 supervisor；或拆成 `app-api` + `app-worker` 两 service |
| 本地 / 服务器 `.env` 漂移 | 连错库、CORS 失败 | 维护 `deploy/.env.example`，与根目录 `.env.example` 分开 |
| 未挂 volume | 数据丢失 | compose 中显式 named volumes + 备份策略 |
| 与开发文档「禁止 compose」混淆 | 新人误在本地起 DB | 本文件 + `deploy/README` 标明仅服务器使用 |

---

## 9. 推荐实施阶段（仍不写代码时的路线图）

### 阶段 A — Codex POC（最小验证）

只起：`postgres` + `redis` + `codex` + `app-worker`（或临时脚本跑 s4）

- 验证：`codex --version`、`/extract` 返回合法 JSON
- 通过后再做全栈

### 阶段 B — 全栈 Compose

补齐 neo4j、minio、frontend、迁移 job、健康检查、volume

### 阶段 C — 硬化（可选）

- HTTPS / 反向代理
- 日志采集（structlog → stdout → `docker logs`）
- 资源 limit、重启策略
- 定期备份 PG / Neo4j

---

## 10. 总体结论

| 维度 | 结论 |
|------|------|
| **方案整体** | **可行**，与现有代码架构高度一致 |
| **混合拓扑** | 合理，优于单容器塞全部进程 |
| **容器内 Codex CLI** | 可行，需优先验证镜像与鉴权 |
| **全栈 Compose** | 适合「单机迁服务器」场景 |
| **Skills** | 继续用 `backend/app/llm/skill_templates/`，无需 IDE skills 路径 |

**最大不确定性**：Codex CLI 在无人值守容器中的安装、鉴权与模型网关连通性。建议将 **Codex 容器 POC** 作为迁移第一步，通过后再接完整 compose 与前端。

---

## 11. 参考

- 架构：`docs/ARCHITECTURE.md` §5 部署架构
- Codex Skills 路线图：`docs/v0.3-codex-skills-roadmap.md`
- 本地启动：`README.md`、`Makefile`
- E2E 验收思路：`backend/tests/test_e2e_live_pipeline.py`
- 配置项：`backend/app/core/config.py`、`.env.example`

---

## 12. 实施路线图（建议按此顺序动手）

### 12.0 着手原则

1. **先验证最大风险**：Codex CLI 在 Linux 容器里能否非交互 `codex exec`。
2. **再铺基础设施**：PG / Redis / Neo4j / MinIO 用 compose 起稳。
3. **最后接应用与前端**：业务代码几乎不用改，主要是镜像、env、启动命令。
4. **所有部署资产放 `deploy/`**，不要动本地隧道开发流程。

### 12.1 建议目录结构（第一步就建好）

```text
deploy/
├── README.md                 # 服务器专用启动说明
├── .env.example              # 服务器 env 模板（服务名，非 localhost）
├── docker-compose.yml        # 全栈（可先用 compose 分文件）
├── docker-compose.infra.yml  # 可选：仅四库，便于分步调试
├── Dockerfile.app            # Python 3.11 + uv + 双进程启动
├── Dockerfile.codex          # 同上 + 安装 codex CLI
├── Dockerfile.frontend       # node build + nginx
├── nginx/
│   └── default.conf          # / → 静态，/api → app:8000
└── scripts/
    ├── app-entrypoint.sh     # uvicorn + celery 同容器
    ├── codex-smoke.sh        # 调 /extract 最小请求
    └── wait-for-it.sh        # 等 postgres/redis 就绪
```

仓库根目录保留 `.env` 给本地；服务器用 `deploy/.env`（`docker compose --env-file deploy/.env`）。

### 12.2 阶段 0 — 服务器前置（半天）

在目标 Linux 上确认：

| 项 | 要求 |
|----|------|
| Docker + Compose v2 | 已安装 |
| 资源 | ≥16GB RAM、≥100GB 磁盘（见 §5.3） |
| 出站网络 | 容器能访问 LLM 网关（Codex / LiteLLM） |
| 代码 | `git clone` 或 rsync 到服务器，例如 `/opt/8d-kg-platform` |

本地先整理 secrets（不要提交 git）：

- `POSTGRES_PASSWORD`、`NEO4J_PASSWORD`、`REDIS_PASSWORD`
- `MINIO_SECRET_KEY`、`SECRET_KEY`
- Codex / LiteLLM 相关 API Key
- 从现有 `.env` 复制可复用项，**把 host 全部改成 compose 服务名**

### 12.3 阶段 A — Codex POC（优先，1～2 天）

**目标**：不跑全站，只证明 `codex` 容器能返回合法 JSON。

**步骤：**

1. 写 `deploy/Dockerfile.codex`：
   - 基础镜像 `python:3.11-slim`（或 `uv` 官方镜像）
   - `pip/uv install` 项目依赖（根目录 `pyproject.toml`）
   - **安装 Codex CLI**（固定版本，写入 Dockerfile 注释记录来源）
   - `WORKDIR` 建议为**仓库根**，`PYTHONPATH=/app/backend`
   - 启动命令：`uvicorn app.codex_service:app --host 0.0.0.0 --port 8787`（在 `backend` 目录或设 PYTHONPATH）

2. 最小 `docker-compose` 只起 `codex`（可临时加 `redis` 若后续要测 worker）：
   - 挂载整个仓库到 `/app`（保证 `skill_templates`、`domain_lexicon.yaml` 可读）
   - 设置 `CODEX_EXEC_WORKDIR=/app`
   - 注入 Codex 鉴权 env 或挂载 `~/.codex` 配置目录（只读）

3. 在服务器执行 smoke：

   ```bash
   docker compose -f deploy/docker-compose.yml up -d codex
   curl -s http://localhost:8787/health
   # 再 POST /extract（可用 scripts/codex-smoke.sh 构造最小 payload）
   ```

4. **通过标准**：HTTP 200，`data` 能被 `ExtractionResult` schema 解析（哪怕内容很少）。

**未通过时**：先不要写 app/frontend 镜像；排查 CLI 登录、模型名、网关网络。可临时设 `LLM_PROVIDER=litellm` 验证 Pipeline 其它部分不受影响。

### 12.4 阶段 B — 基础设施 compose（1 天）

**目标**：四库健康检查通过，迁移可跑。

1. 在 `deploy/docker-compose.yml` 增加：
   - `postgres:15`（+ pgvector 若需要，用 `pgvector/pgvector` 镜像）
   - `neo4j:5-community`
   - `redis:7`
   - `minio/minio` + 创建 bucket 的 init 或文档说明

2. 为每个服务配置：
   - `healthcheck`
   - `volumes`（`pg_data`、`neo4j_data`、`minio_data`、`redis_data`）
   - 仅内网暴露端口；需要外网访问的只暴露 `frontend:80`

3. 增加一次性服务 `migrate`：

   ```bash
   cd /app/backend && alembic upgrade head
   ```

   `depends_on: postgres` 且 `condition: service_healthy`。

4. 验收：

   ```bash
   docker compose -f deploy/docker-compose.yml up -d postgres neo4j redis minio
   docker compose -f deploy/docker-compose.yml run --rm migrate
   ```

### 12.5 阶段 C — 应用层（1～2 天）

**目标**：`app` 容器 API + worker 跑通，`GET /api/v1/health` 四项 ok。

1. 写 `deploy/Dockerfile.app`（与 codex 共用 Python 依赖层可抽 `Dockerfile.python-base`）。

2. **混合拓扑的两种实现**（二选一）：

   | 方式 | compose 写法 | 优点 |
   |------|--------------|------|
   | **A. 单 service 双进程** | 一个 `app`，`entrypoint.sh` 起 uvicorn + celery | 符合「app 一容器」语义 |
   | **B. 同镜像两 service** | `app-api` + `app-worker`，`command` 不同 | 重启、扩缩互不影响，**更推荐** |

3. `app` 环境变量（关键）：

   ```bash
   CODEX_BASE_URL=http://codex:8787
   DATABASE_URL=postgresql+asyncpg://...@postgres:5432/kg
   NEO4J_URI=bolt://neo4j:7687
   REDIS_URL=redis://:pass@redis:6379/0
   CELERY_BROKER_URL=redis://:pass@redis:6379/1
   MINIO_ENDPOINT=minio:9000
   ```

4. **注意**：`backend/app/core/config.py` 启动时 `assert` 要求仓库根存在 `.env`。compose 里用：

   ```yaml
   env_file: deploy/.env
   volumes:
     - ./.env:/app/.env:ro   # 或在镜像构建时 COPY deploy/.env
   ```

   长期可改为「无文件时仅读环境变量」，但 POC 阶段挂载 `.env` 最快。

5. 验收：

   ```bash
   docker compose up -d app codex
   curl -s http://localhost:8000/api/v1/health | jq .
   ```

### 12.6 阶段 D — 前端（半天～1 天）

1. `Dockerfile.frontend`：`npm ci && npm run build`，nginx 托管 `dist/`。
2. `nginx` 配置：
   - `location /api/` → `http://app-api:8000`（或 `app:8000`）
   - `location /` → 静态文件
3. 生产构建可不设 `VITE_API_BASE_URL`（前端默认 `/api/v1`）。
4. 图谱页若直连 Neo4j Bolt，需单独配置 `VITE_NEO4J_*` 或通过后端代理（安全上更建议仅内网开放 Neo4j）。

### 12.7 阶段 E — 端到端验收（半天）

与 `backend/tests/test_e2e_live_pipeline.py` 对齐：

1. 上传 `docs_for_test/` 下 EP2002 样例 docx（经 UI 或 API）。
2. 观察 Celery 日志：`s4_extract` → `codex_request` → `s6_write`。
3. 查 Neo4j：`EightDReport` 节点、`occurred_at` 等字段。
4. 前端搜索「克诺尔苏州」等关键字有结果。

可选：在服务器跑

```bash
cd backend && uv run pytest -m integration tests/test_e2e_live_pipeline.py
```

（需把测试里的 host 改为 compose 服务名或 host 网络模式。）

### 12.8 实施顺序一览（ checklist ）

```text
[ ] 0. 创建 deploy/ 目录与 .env.example
[ ] A. Dockerfile.codex + codex smoke 通过
[ ] B. infra compose + migrate 通过
[ ] C. Dockerfile.app + health 四项 ok
[ ] D. frontend + nginx 反代
[ ] E. 上传样例报告全链路成功
[ ] F. （可选）备份 volume、restart policy、资源 limit
```

### 12.9 建议本周最小可交付（MVP）

只做能让**一条报告跑通**的最小集合：

| 包含 | 暂不包含 |
|------|----------|
| postgres, redis, neo4j, minio | HTTPS / 域名 |
| codex + app-api + app-worker | K8s |
| migrate job | 监控大盘 |
| 手动 `curl` 验收 + 一次 UI 上传 | 自动 CI 部署 |

### 12.10 可能需要的小幅代码调整（实施时再改）

| 项 | 原因 | 优先级 |
|----|------|--------|
| `config.py` 去掉 `.env` 硬 assert | 容器仅用 `env_file` 注入时更灵活 | 低（可先挂载文件） |
| `Makefile` 增加 `deploy-up` / `deploy-logs` | 服务器运维方便 | 低 |
| 健康检查依赖 `strict_startup` | 启动顺序更稳 | 低 |

**当前业务 Pipeline / Codex / Skills 无需为部署重写。**

---

**文档版本**：v0.2（含实施路线图）  
**最后更新**：2026-05-26
